# v2.0 GEMM — Rung 1 Evidence (Foundation + De-risk)

Branch `v2.0/gemm`. Spec: `docs/superpowers/specs/2026-06-29-v2.0-gemm-rung1-design.md`.

## Goal
Answer honestly, before investing in fused epilogues or engine changes: can the
engine generate **correct** tiled GEMM, and what fraction of cuBLAS does it reach?

## Local (no hardware)
- `evals/gemm/` suite: `matmul_fp16`, `matmul_fp32`, `matmul_bias_gelu_fp16`
  (all square; tiling/tensor-core guidance in the fixture prompts).
- `tests/unit/test_gemm_suite.py`: structure + no-overlap + finite-output checks — PASS.
- `cuda-engine eval --suite gemm` wired.
- Zero engine code changes (pure experiment).

## Hardware spike (credit-gated, Colab A100) — TO FILL
Run on branch `v2.0/gemm`:
```
cuda-engine eval --suite gemm --out "$OUT_DIR/gemm-rung1" --no-resume --yes
```

Colab A100 spike run 2026-07-02 (`gemm-rung1`): **3/3 PASS correctness.**

| Fixture | Compiled? | Correct vs torch.matmul? | Speedup vs cuBLAS | Notes |
|---|---|---|---|---|
| matmul_fp16 | yes | ✅ | **0.06×** | correct CUDA-core tiled GEMM; ~16× slower than cuBLAS = the tensor-core gap (cuBLAS fp16 uses tensor cores). |
| matmul_fp32 | yes | ✅ | **0.91×** | near cuBLAS on CUDA-core fp32 — strong. |
| matmul_bias_gelu_fp16 | yes | ✅ | **1.11×** | fused epilogue beats torch matmul+gelu — **PROMISING but UNVERIFIED (see below).** |

## Correctness gate: PASSED
3/3 correct, including a fused 3-input GEMM. The engine can generate correct
tiled GEMM — the core de-risk is resolved positively.

## ⚠️ Open question before committing Rung 2: is the 1.11× fused speedup real?
It is internally inconsistent with the 0.06× bare fp16 result: GEMM cost is
O(N³) matmul, fusion only saves an O(N²) round-trip, so a non-tensor-core fused
kernel should NOT beat a cuBLAS-backed fused baseline. Two very different
explanations, needing the artifacts to distinguish:
1. the fused kernel used tensor cores (WMMA) while the bare one didn't → a
   genuine fused+tensor-core win (great), OR
2. the fused baseline was measured against something slow (a baseline artifact,
   like the v1.0 bug) → 1.11× is not real.
**Verify before Rung 2:** inspect `matmul_bias_gelu_fp16` `stage5_polish/.../kernel.cu`
(does it use `wmma`/`mma`?) and the `stage4_performance/.../benchmark.json`
(`baseline_mode`, `baseline_ms`, `custom_ms`, benchmark shape) for all three.

## RESOLVED via benchmark.json (2026-07-02): the speedups are NOT trustworthy
Tensor-core markers: `matmul_fp16` + `matmul_fp32` USE wmma; the fused
`matmul_bias_gelu_fp16` does NOT (CUDA cores only). benchmark.json:
- `matmul_fp16`: custom 10.02ms vs baseline 0.647ms (cuBLAS) = 0.06× — a REAL but
  naive WMMA kernel (~4% of A100 fp16 tensor peak; achieved 10 GB/s).
- `matmul_fp32`: baseline_ms **None** (failed).
- `matmul_bias_gelu_fp16`: baseline_ms **None**; custom 0.952ms — **physically
  impossible** for a real 4096³ CUDA-core GEMM (=144 TFLOP/s > ~78 TFLOP/s fp16
  CUDA-core peak, no tensor cores) → the kernel is fast-but-wrong at the 4096²
  PERF shape (correctness only verified at ≤1024²).

Two harness problems make every GEMM speedup noise:
1. **baseline None** — `torch.compile` of a 4096² GEMM trips the 60s benchmark
   subprocess timeout (`local.py`), so the baseline intermittently fails; reported
   1.11×/0.91× came from other attempts and aren't reproducible.
2. **perf shape (4096²) >> correctness shapes (≤1024²)** — a kernel can pass the
   correctness gate yet be fast-and-wrong at the perf shape (the impossible 0.952ms).

## Decision gate (OUTCOME)
- ✅ Correctness de-risk PASSED (3/3 correct at verified shapes; WMMA emission
  works for fp16+fp32). The hard unknown is resolved positively.
- ❌ Perf harness NOT ready for GEMM.
- **Rung 2 = make GEMM perf measurement trustworthy FIRST**, then fused epilogues:
  (a) raise/handle the baseline benchmark timeout for GEMM; (b) benchmark at a
  shape the correctness gate verifies (or verify correctness at the perf shape) so
  fast-but-wrong kernels can't post fake wins; (c) THEN re-measure fused epilogues
  + improve naive WMMA quality (fp16 WMMA at 4% of peak). Do NOT build fused-win
  claims on the current harness.

## Rung 2 VALIDATED (2026-07-02, credit-free re-benchmark of the Rung 1 kernels)
Recompiled the saved Rung 1 `.cu` (no LLM) and re-benchmarked with the Rung 2 harness on Colab A100:
- `matmul_fp32`: **benchmark_correct=False** (max_abs_err 5.8e17), baseline_ms **7.32ms** (was None). Rung 1 "0.91×" was FAKE — wrong at 4096².
- `matmul_bias_gelu_fp16`: **benchmark_correct=False** (max_abs_err 769), baseline_ms **0.70ms** (was None). Rung 1 "1.11×" was FAKE — wrong at 4096².
- `matmul_fp16`: saved polished `.cu` failed to recompile in a fresh env ("anyset" parse error) — separate artifact quirk; not re-tested.

=> Rung 2 does its job: catches fast-but-wrong kernels (both fake GEMM "wins" flagged) AND fixes the baseline timeout (baselines now measure). Confirmed the Rung 1 GEMM speedups were all noise. The REAL GEMM problem is correctness-at-scale (pass ≤1024², break at 4096²) — Rung 3 must verify at the perf shape in codegen/correctness so the engine fixes large-N bugs.

# v2.2 — Export / torch.compile compatibility — Evidence

Status: **hardware-validated on A100 (2026-08-09).** The export loop works end to end.

## Hardware validation — 2026-08-09

Device `NVIDIA A100-SXM4-40GB`, torch `2.11.0+cu128`, Colab.
Run `23b43cf0f48e` = `matmul_bias_gelu_fp16` from the `gemm-rung4` eval — the fused
CUDA-core epilogue that carries the v2.0 thesis.

Command (consumes an existing run; **no API credits**):

```
python tools/export_validation/validate_export.py \
    --run-id 23b43cf0f48e \
    --runs-root /content/drive/MyDrive/cuda-engine-evals/gemm-rung4/artifacts/matmul_bias_gelu_fp16
```

Output:

```
exported package: ce_fused_gemm_bias_gelu_epilogue
  shipped kernel : stage2_codegen/final/kernel.cu
  nvcc flags     : ['-O3', '--use_fast_math']
  verified       : True

Building wheel for ce-fused-gemm-bias-gelu-epilogue (pyproject.toml) ... done
Successfully installed ce-fused-gemm-bias-gelu-epilogue-0.1.0

--- standalone check (fresh process, no cuda_engine) ---
device: NVIDIA A100-SXM4-40GB  torch: 2.11.0+cu128
  PASS  package imports without cuda_engine
  PASS  manifest read -- run=23b43cf0f48e flags=['-O3', '--use_fast_math']
  PASS  kernel builds and runs -- loaded=JIT source build
  PASS  matches reference -- rtol=0.01 atol=0.01 size=256
  PASS  control: comparison rejects a wrong answer
  PASS  torch.compile(fullgraph=True) traces it -- no graph break
  PASS  compiled result matches eager

ALL 7 CHECKS PASSED
EXPORT VALIDATION PASSED -- the export loop works end to end.
```

Exit code 0.

The control line matters: the correctness comparison was shown to reject a
deliberately perturbed answer in the same run, so the `matches reference` pass is
not vacuous.

## All three gemm-rung4 kernels — 2026-08-09

Re-run across every exportable run on Drive after fixing the control (below):

```
SUMMARY
  matmul_bias_gelu_fp16: PASS
  matmul_fp16:           PASS
  matmul_fp32:           PASS
```

`matmul_fp32` matters most: its accepted kernel is recorded at
`stage4_performance/perf_repair/attempt_01/kernel.cu`, a path no candidate list
contains. It is the only on-hardware exercise of the recorded-artifact lookup, and
it would have been permanently unexportable without it.

**These three passes are about the export loop, not kernel quality.** All three
come from the `gemm-rung4` eval, whose WMMA *guidance* was subsequently reverted as
a net regression. `matmul_fp16` in particular is the naive-WMMA kernel that is
correct but sits at ~0.11× — exporting and validating cleanly says the packaging
works, not that the kernel is good. Do not read "3/3 export validation passed" as
"3/3 good kernels".

## The control caught a defect in itself

On the first `matmul_fp32` run the control fired:
`control failed: comparison accepts a deliberately wrong answer`.

It was right, and the defect was in the harness. `_matches` uses `torch.allclose`,
which compares against `atol + rtol*|expected|`. The control perturbed the expected
output by a fixed `+1.0`, but this GEMM's outputs are ~1e11 at `size=256`, so with
`rtol=1e-3` the band is ~1e8 — the nudge was about 1e8 times too small to detect.
The control correctly reported it could not distinguish, which also meant the
`matches reference` pass above it was weak evidence at that magnitude.

The kernel was never in question. The engine's own report for this run:

| shape | max_abs_err | max_rel_err |
|---|---|---|
| 256² | 3.3e5 | 1.5e-6 |
| 512² | 1.5e7 | 2.3e-6 |
| 1024² | 1.4e9 | 4.1e-6 |
| 4096² | 3.5e13 | 1.8e-4 |

against `rtol=1e-3`. The absolute errors look catastrophic and are meaningless on
their own — outputs reach ~1e17. Only the relative column carries information,
which is why the check now reports `max_rel_err` rather than a bare `PASS`.

`_perturb` now scales with the value plus an absolute floor. A regression test
pins the *old* behaviour too — asserting `+1.0` really was inside the band at 2e11
— so reverting to an additive nudge fails with an explanation rather than silently.

## What this proves

| Claim | Status |
|---|---|
| A run directory can be exported to an installable package | ✅ A100, 3/3 runs |
| The package builds a wheel and `pip install`s | ✅ A100 |
| It imports with `cuda_engine` absent from the process | ✅ A100 |
| The kernel JIT-builds from shipped source and runs | ✅ A100 |
| The exported kernel matches the PyTorch reference | ✅ A100 |
| `torch.compile(fullgraph=True)` traces it — no graph break | ✅ A100 |
| …and fails without the fake impl (negative control) | ✅ CPU unit test |
| The compiled result equals the eager result | ✅ A100 |

## What this does NOT prove

- **Three kernels, one architecture.** All sm_80, all from one eval. No coverage of
  elementwise/reduction kernels, which are the engine's actual strength.
- **The prebuilt `.so` path is untested.** Polish was rejected on this run, so no
  `.so` existed and the JIT source build ran. The `torch.ops.load_library` branch
  and its fallback have never executed.
- **Performance was not re-measured after export.** `VERIFICATION.md` reports the
  1.25× from the original run; the exported package was checked for *correctness*,
  not for still hitting that number. An exported kernel could in principle build
  differently and be slower. Closing this needs a benchmark in the standalone check.
- **Only one exported package per process.** The op registers under the fixed
  `cuda_engine::forward` namespace, so a second package's `TORCH_LIBRARY`
  registration collides. Needs a per-package namespace before anyone ships several.
- **Shape coverage.** Checked at 256 bound to every symbolic dim, not at the
  correctness-suite shapes or the 4096² benchmark shape.

## Defects found by building and running this — five

None were reachable from the unit suite.

1. **`python -m cuda_engine.cli` silently did nothing and exited 0** — no
   `__main__` guard. The driver's first draft used it, so export would have
   "succeeded" while producing no package. Fixed; the driver now confirms the
   artifact exists rather than trusting an exit code.
2. **The exported package ignored the run's nvcc flags**, JIT-building with
   torch's defaults instead of the `-O3 --use_fast_math` the kernel was measured
   with — so the installed kernel would not have been the one `VERIFICATION.md`
   describes. Flags now travel in `manifest.json` and are used by the loader.
3. **The kernel-source candidate paths were wrong.** `codegen.py:90` writes to
   `{artifact_prefix}/final/kernel.cu`, not `stage2_codegen/kernel.cu`. Three real
   run directories were all rejected as "missing kernel.cu" while containing
   kernels. The unit tests could not catch it — the fixtures wrote to the same
   wrong paths the implementation read from, so they agreed and both were wrong.
   `_select_kernel` now reads the accepted path the run recorded in
   `checkpoint.json`, which also covers repair and escalation kernels that no
   fixed list can enumerate. (`matmul_fp32` records its kernel under
   `stage4_performance/perf_repair/attempt_01/` — unexportable without this.)
4. **`ninja` was not declared as a dependency**, so the default source-build path
   failed on first use with an opaque torch error. Now declared, and build
   failures are re-raised with what to do about them.
5. **The harness's own control was too weak to fire** at GEMM magnitudes — a fixed
   `+1.0` perturbation against a relative tolerance. Caught by the control itself
   refusing to certify a pass it could not distinguish. Fixed to scale with the
   value; the correctness line now reports error magnitudes instead of a boolean.

Defects 3 and 4 were only reachable by pointing the tool at real run directories
on real hardware. That is the argument for this harness existing.

## Reproducing

```
python tools/export_validation/validate_export.py --run-id <id> --runs-root <dir>
```

Or `examples/export_validation.ipynb` on Colab. Verify by **exit code**.

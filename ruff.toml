line-length = 100
target-version = "py311"

[lint]
select = ["E", "F", "I", "B", "UP", "SIM", "RUF"]
ignore = ["E501"]
# "×" is the project's convention for speedups in docs and generated markdown
# (README, CHANGELOG, VERIFICATION.md), not a typo for the letter x.
allowed-confusables = ["×"]
